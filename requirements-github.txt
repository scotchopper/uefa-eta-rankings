# Minimal requirements for GitHub Actions
# Only essential packages needed for UEFA processing

# Core functionality
requests>=2.28.0

# JSON and data processing
# (json is built-in to Python)

# File handling
# (pathlib, os, datetime are built-in)

# Optional: Data processing if needed
pandas>=1.5.0
openpyxl>=3.0.0